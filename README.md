# Firebase setup for HR RIDE V15
Enable Authentication > Phone, Cloud Firestore and Cloud Messaging in project `hr-ride`.
Deploy `firestore.rules` from this folder.
For iOS push notifications, configure APNs in Firebase.
